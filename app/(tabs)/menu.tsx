import { View, Text } from 'react-native';

export default function MenuScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-black">
      <Text className="text-white text-lg font-bold">Sama Menu</Text>
    </View>
  );
}